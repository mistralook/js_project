<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Миллионер</title>
    <link rel="stylesheet" href="/css/leader_style.css">
    <link rel="shortcut icon" href="/images/favicon.ico" type="image/x-icon">
</head>
<body>

<table border="1" cellpadding="5">
    <tr>
        <th>Место</th>
        <th>Имя</th>
        <th>Очки</th>
    </tr>
    <% for (var i = 0; i < n-1 ; i++) { %>
        <tr>
            <td><%= data[i].place %></td>
            <td><%= data[i].name %></td>
            <td><%= data[i].score %></td>
        </tr>
    <% } %>
    <% if(!inTop) {%>
        <tr>
            <td>...</td>
            <td>...</td>
            <td>...</td>
        </tr>
    <% } %>
    <tr>
        <td><%= data[n-1].place %></td>
        <td><%= decodeURI(data[n-1].name) %></td>
        <td><%= data[n-1].score %></td>
    </tr>

</table>
    <form  method="get" action="/game">
        <button id = "newStart" type="submit" name="name" value=<%-JSON.stringify(decodeURI(userName))%> > Начать заново</button>
    </form>
<script>
    const userName = <%-JSON.stringify(decodeURI(userName))%>;
    const td =[...document.getElementsByTagName("td")].filter(item => item.innerText === userName)[0];
    [td.parentElement][0].style.background="#e0b139";
</script>
</body>
</html>